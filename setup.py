from setuptools import setup, find_packages

setup(
    name='PackagedAssignment4',  # Replace with your desired package name
    version='0.1.0',  # Update this version number as needed (follow semantic versioning)
    packages=find_packages(),
    install_requires=[
        # List your dependencies here if any
    ],
)
